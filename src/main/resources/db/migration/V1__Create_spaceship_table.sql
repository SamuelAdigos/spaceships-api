CREATE TABLE spaceship (
                           id IDENTITY PRIMARY KEY,
                           name VARCHAR(255),
                           franchise VARCHAR(255),
                           max_speed DOUBLE
);